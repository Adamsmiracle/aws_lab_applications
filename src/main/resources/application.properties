spring.application.name=photo_uploader
server.port=8080

# Create the photos table on startup (schema.sql). Idempotent.
spring.sql.init.mode=always

# Upload limits
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
